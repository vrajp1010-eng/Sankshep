# PageFlow - AI Provider Configuration
# Only Google Gemini is used. Set your Gemini API key below.

# Google Gemini (required)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Default provider (always gemini)
DEFAULT_PROVIDER=gemini
